tasks:
  - init: npm install
    command: npm run dev

ports:
  - port: 8080
    onOpen: open-preview

vscode:
  extensions:
    - esbenp.prettier-vscode
    - dbaeumer.vscode-eslint
    - svelte.svelte-vscode
